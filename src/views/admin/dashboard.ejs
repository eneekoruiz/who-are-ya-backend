<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admuinistrazio panela</title>
    <style>
        body { font-family: Arial; margin: 0; padding: 20px; background: #f5f5f5; text-align: center;}
        .header { background: #2c3e50; color: white; padding: 15px; border-radius: 5px; }
        .menu { background: white; padding: 15px; margin: 15px 0; border-radius: 5px; }
        .menu a { margin-right: 15px; color: #3498db; text-decoration: none; }
        .user-info { background: #ecf0f1; padding: 10px; margin-bottom: 15px; border-radius: 5px; }
        .card { background: white; padding: 20px; margin: 15px 0; border-radius: 5px; box-shadow: 0 2px 5px rgba(0,0,0,0.1); }
        .menu button { font-size: 1.2em; padding: 12px 24px; border-radius: 6px; cursor: pointer; }
    </style>
</head>
<body>
    <div class="header">
        <h1>Administrazio panela - Who Are Ya</h1>
    </div>
    
    <div class="user-info">
        <strong>Erabiltzailea:</strong> <%= user.username %> | 
        <strong>Email:</strong> <%= user.email %> | 
        <strong>Rola:</strong> <%= user.role %> |
        <a href="/login/" onclick="return confirm('Saioa amaitu?')" style="color: #e74c3c;">Logout</a>
    </div>
    
    <div class="menu">
        <a href="/admin/players"><button type="button">Ikusi jokalariak</button></a>
        <a href="/admin/players/new"><button type="button">Jokalari berria</button></a>
        <a href="/api/players"><button type="button">Jokalari API ikusi</button></a>
    </div>
    
    <script>
        if ('<%= user.role %>' !== 'admin') {
            alert('Error: Administrazio baimenik ez');
            window.location.href = '/api/auth/login-page';
        }
    </script>
</body>
</html>